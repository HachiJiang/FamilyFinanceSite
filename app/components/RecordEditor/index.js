/*
 * RecordEditor
 *
 * Edit single record, add/remove/modify
 */

import React from 'react';
import { FormattedMessage } from 'react-intl';

const RecordEditor = props => (
    <div className="record-editor">
        This is RecordEditor!
    </div>
);

export default RecordEditor;